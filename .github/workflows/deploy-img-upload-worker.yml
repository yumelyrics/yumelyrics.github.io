# Deploy Cloudflare Worker untuk upload cover ke ImgBB.
# GitHub Secrets:
#   CF_API_TOKEN   — sudah ada
#   CF_ZONE_ID     — sudah ada (dipakai ambil Account ID)
#   IMGBB_API_KEY  — dari https://api.imgbb.com/

name: Deploy ImgBB Upload Worker

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'cloudflare-worker/**'
      - '.github/workflows/deploy-img-upload-worker.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cloudflare-worker
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Resolve Cloudflare Account ID
        id: cf
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          if [ -z "$CF_API_TOKEN" ]; then
            echo "::error::Secret CF_API_TOKEN belum di-set"
            exit 1
          fi
          ACCOUNT_ID=$(node <<'NODE'
          const token = process.env.CF_API_TOKEN;
          const zoneId = process.env.CF_ZONE_ID || '';
          const preset = process.env.CLOUDFLARE_ACCOUNT_ID || '';

          async function cfGet(url) {
            const res = await fetch(url, {
              headers: { Authorization: 'Bearer ' + token },
            });
            const json = await res.json().catch(() => ({}));
            if (!res.ok || !json.success) {
              throw new Error('Cloudflare API gagal: ' + JSON.stringify(json));
            }
            return json;
          }

          (async () => {
            if (preset) {
              console.log(preset);
              return;
            }
            if (zoneId) {
              const z = await cfGet('https://api.cloudflare.com/client/v4/zones/' + zoneId);
              const id = z.result?.account?.id;
              if (id) {
                console.log(id);
                return;
              }
            }
            const a = await cfGet('https://api.cloudflare.com/client/v4/accounts');
            const id = a.result?.[0]?.id;
            if (id) {
              console.log(id);
              return;
            }
            console.error('Account ID tidak ketemu. Token mungkin tidak punya akses Workers.');
            console.error('Solusi: tambah secret CLOUDFLARE_ACCOUNT_ID (dari Cloudflare dashboard → Workers).');
            process.exit(1);
          })().catch((e) => {
            console.error(e.message || e);
            process.exit(1);
          });
          NODE
          )
          echo "account_id=$ACCOUNT_ID" >> "$GITHUB_OUTPUT"

      - name: Install Wrangler
        run: npm install

      - name: Set ImgBB secret on Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ steps.cf.outputs.account_id }}
          IMGBB_API_KEY: ${{ secrets.IMGBB_API_KEY }}
        run: |
          if [ -z "$IMGBB_API_KEY" ]; then
            echo "::error::Secret IMGBB_API_KEY belum di-set"
            exit 1
          fi
          printf '%s' "$IMGBB_API_KEY" | npx wrangler secret put IMGBB_API_KEY

      - name: Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ steps.cf.outputs.account_id }}
          command: deploy
          workingDirectory: cloudflare-worker

      - name: Health check
        run: |
          sleep 3
          curl -fsS "https://rough-snowflake-e4dc.khoirustsani143.workers.dev/health" | tee health.json
          node -e "const j=require('./health.json'); if(!j.ok||!j.imgbb_configured) process.exit(1)"
